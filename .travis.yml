language: php

sudo: false

cache:
  directories:
    - $HOME/.composer/cache

addons:
  apt_packages:
    - parallel

php:
  - 5.3.3
  - 5.3
  - 5.4
  - 5.5
  - 5.6
  - 7.0
  - hhvm
  - hhvm-nightly

before_script:
    - composer install
